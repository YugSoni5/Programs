#include<stdio.h>
#include<string.h>

int main(int argc, char const *argv[])
{
    char str [20] = "happy birthday";
    printf("Orignal string is %s", str);
    printf("String after strupr %s", str);
    return 0;
}
