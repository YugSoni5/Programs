#include <stdio.h>

int main()

{
    int i;
    printf("prees 0:");
    scanf("%d", &i);
    if (i == 0)
    {
        printf("Zero is pressed");
    }
    else
    {
        printf("zero is not pressed");
    }

    return 0;
}